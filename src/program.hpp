/**
 * @file program.hpp
 * @brief Declaration of the main program functions.
 */

#ifndef PROGRAM_HPP
#define PROGRAM_HPP

struct GLFWwindow;

/**
 * @brief Starts the main rendering loop and initializes scene components.
 * @param window Pointer to the GLFW window context.
 */
void runProgram(GLFWwindow* window);

#endif